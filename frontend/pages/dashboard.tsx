import AppShell from "@/components/Shell";

export default function DashboardPage() {
  return (
    <AppShell pageTitle="Dashboard">
      <div style={{ padding: 28 }}>
        {/* Dashboard content — implemented in Metadata Explorer phase */}
        <p style={{ color: "#7A7068", fontSize: 14 }}>Dashboard — coming soon.</p>
      </div>
    </AppShell>
  );
}
